# إعداد وتجهيز Paymob Payment Integration في THOTH

هذا المستند يشرح بالتفصيل كيفية تشغيل وإعداد بوابة دفع "Paymob" بنجاح داخل مشروع THOTH الجديد.

## 1. الحصول على بيانات الربط (Credentials)
1. قم بتسجيل الدخول إلى حسابك في [Paymob Dashboard](https://accept.paymob.com/).
2. توجه إلى قسم **Settings** (الإعدادات) في القائمة الجانبية.
3. ستجد مفاتيح الربط (Workspace Keys):
   - **Secret Key** (أو API Key)
   - **Public Key**
   - **HMAC Secret** (للتأكد من صحة العمليات الواردة)
4. توجه إلى قسم **Developers > Payment Integrations**.
5. قم بإنشاء وسيلة دفع جديدة (مثلاً بطاقات الائتمان، فوري، محافظ إلكترونية) وانسخ الـ **Integration ID** الخاص بها.

## 2. وضع الإعدادات في البيئة (Environment Variables)
في بيئة السيرفر (Production) أو في ملف `.env`، قم بإضافة المتغيرات التالية ولا تقم بوضعها إطلاقاً في الواجهة الأمامية:

```env
PAYMOB_API_KEY="sk_..."
PAYMOB_PUBLIC_KEY="pk_..."
PAYMOB_INTEGRATION_ID="1234567,9876543" 
PAYMOB_HMAC_SECRET="HMAC_..."
PAYMOB_TEST=true # اجعلها false عند الإطلاق الفعلي
```

> **ملاحظة:** إذا كان لديك أكثر من Integration ID (مثلاً بطاقات ائتمان ومحافظ ذكية)، يمكنك الفصل بينهم بفاصلة `,` في المتغير `PAYMOB_INTEGRATION_ID`.

## 3. إعداد Webhook لتأكيد الدفع التلقائي
**مهم جدًا:** النظام مبني بحيث لا يقوم بتفعيل الاشتراك أبداً إلا بعد وصول رسالة التأكيد الصامتة من Paymob لتجنب التلاعب.

1. داخل Paymob Dashboard، توجه إلى **Developers > Webhooks**.
2. أضف Webhook جديد من نوع **Transaction Processed**.
3. ضع الرابط كالتالي (استبدل النطاق الخاص بك):
   `https://[YOUR_DOMAIN]/api/payment/paymob/webhook`
4. تأكد من تفعيل الـ Webhook.

## 4. إعداد الاشتراكات الشهرية والسنوية
تم دمج خطط الدفع بحيث ترتبط بخطط THOTH بشكل تلقائي:
- **Monthly**: يتم تفعيل الاشتراك وتعيين تاريخ الانتهاء (`expires_at`) بعد شهر.
- **Yearly**: يتم التفعيل وإضافة سنة كاملة لصلاحية الحساب.
- النظام يعتمد على `Plan_ID` الممرر (مثل `ultra_yearly`) ويقوم بتحديث الصلاحيات بمرونة.

## 5. اختبار وضع التطوير (Test Mode)
1. اجعل `PAYMOB_TEST=true` في إعدادات البيئة.
2. استخدم [بطاقات الاختبار الرسمية من Paymob](https://docs.paymob.com/docs/testing-payment-methods).
3. اختر الباقة، اضغط على زر الشراء عبر Paymob (سيتم عرض نافذة iframe مدمجة ضمن THOTH بشكل سلس وأنيق).
4. بعد نجاح الدفع الوهمي، سيقوم Webhook بالاتصال بالسيرفر، وسيتغير حسابك للرتبة الجديدة تلقائياً.

## 6. تفعيل الباقات الحقيقية (Production Mode)
عند الإطلاق الرسمي:
1. تأكد من تغيير `PAYMOB_TEST=false`.
2. استبدل مفاتيح `sk_test` و `pk_test` بمفاتيح الـ Live من Paymob.
3. قم بتغيير الـ Integration IDs لوسائل الدفع الحقيقية المعتمدة (التي تم مراجعتها من فريق Paymob).

## 7. أين يتم تفعيل الباقات وتحديث قاعدة البيانات؟
تم إنشاء نظامين في السيرفر:
1. **جدول `paymentOrders`**: لتسجيل المحاولات مبدئياً بحالة `pending`.
2. **جدول `subscriptions`**: يتم الكتابة فيه وإضافة حالة `active` ورقم الـ Transaction بمجرد استلام الـ Webhook. كما يتم تحديث حقل `plan` في مستند المستخدم `users` الخاص بحدود تطبيق THOTH.

---

إذا واجهت أي مشاكل، تأكد من أن الـ `PAYMOB_HMAC_SECRET` مطابق تماماً لما هو في Paymob لضمان قراءة التوقيع الرقمي بنجاح!
